name: trasnlator
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_launcher_icons: ^0.14.3
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  translator: ^1.0.3+1
  sizer: 3.0.4

flutter_icons:
  android: true
  image_path: "assets/trasnlate_image.jpg"

flutter:
  uses-material-design: true
